{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": "src",
    "outDir": "dist",

    // consumed by the apps via project references, so ship types
    "composite": true,
    "declarationMap": true
  },
  "include": ["src/**/*"]
}
