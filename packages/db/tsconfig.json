{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": "src",
    "outDir": "dist",

    // consumed by the apps, so ship types
    "declaration": true,
    "declarationMap": true
  },
  "include": ["src/**/*"]
}
